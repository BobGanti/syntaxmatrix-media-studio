# SyntaxMatrix Media Studio Frontend

Frontend views for SyntaxMatrix Media Studio.

The current direction is feature-by-feature pages:

```text
/tasks/voice-clone
/admin/voice-clone
```

The frontend talks to Flask feature controllers. Controllers own the backend contract and decide which service module they need.
